<h2>Listing calendars</h2>

<table class="table">
  <thead>
    <tr>
      <th>Origin ID</th>
      <th>Summary</th>
      <th>Time zone</th>
      <th>Access role</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for calendar <- @calendars do %>
    <tr>
      <td><%= calendar.origin_id %></td>
      <td><%= calendar.summary %></td>
      <td><%= calendar.time_zone %></td>
      <td><%= calendar.access_role %></td>

      <td class="text-right">
        <%= link "Pick", to: calendar_path(@conn, :show, calendar), class: "btn btn-default btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New calendar", to: calendar_path(@conn, :new) %>
